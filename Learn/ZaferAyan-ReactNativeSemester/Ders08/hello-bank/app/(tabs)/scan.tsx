import { Text, View } from 'react-native';

const Scan = () => {
  return (
    <View>
      <Text className="text-xl font-semibold text-gray-900">Scan</Text>
    </View>
  );
};

export default Scan;
